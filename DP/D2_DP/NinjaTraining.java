package Restart.DP.D2_DP;

public class NinjaTraining {
    public int maximumPoints(int arr[][]) {
        return helper(arr,0,0);
    }
    private int helper(int[][] arr, int index){
        int max=0;
        int Running=arr[index][0]+Math.max(helper(arr, index))
        int Fighting=
        int Learning=
    }
}
